<Window x:Class="Take_Away_Client.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:utils="clr-namespace:Take_Away_Client.Utils"
        xmlns:local="clr-namespace:Take_Away_Client.View"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="150"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="150"/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>
        <Label Content="All products" Grid.Row="1" Grid.Column="0"/>
        <ListView x:Name="ProductGrid" Grid.Row="1" Grid.Column="0" ItemsSource="{Binding Products, Mode=TwoWay}" SelectedItem="{Binding SelectedProduct}" Margin="10">
            <ListView.View>
                <GridView x:Name="gridAllProducts">
                    <GridViewColumn Header="Product name" DisplayMemberBinding="{Binding productName}" Width="100"/>
                    <GridViewColumn Header="Product price" DisplayMemberBinding="{Binding productPrice}" Width="100"/>
                    <GridViewColumn Header="Product type" DisplayMemberBinding="{Binding productType}" Width="100"/>
                </GridView>
            </ListView.View>
        </ListView>
        <Label Content="selected products" Grid.Row="2" Grid.Column="0"/>
        <ListView x:Name="selectedProductGrid" Grid.Row="3" Grid.Column="0" ItemsSource="{Binding SelectedProducts, Mode=TwoWay}" SelectedItem="{Binding SelectedProduct}" Margin="10">
            <ListView.View>
                <GridView x:Name="gridSelectedProducts">
                    <GridViewColumn Header="Product name" DisplayMemberBinding="{Binding productName}" Width="100"/>
                    <GridViewColumn Header="Product price" DisplayMemberBinding="{Binding productPrice}" Width="100"/>
                    <GridViewColumn Header="Product type" DisplayMemberBinding="{Binding productType}" Width="100"/>
                </GridView>
            </ListView.View>
        </ListView>
        <Button Content="Add" Grid.Row="5" Grid.Column="0" Name="btnAdd" Command="{Binding Path=AddCommand}" Width="100"/>
    </Grid>
    

</Window>
